{
  "compilerOptions": {
    "target": "ES2020",               // Versión de JavaScript de salida
    "module": "commonjs",             
    "outDir": "./dist",               // ⚠️¡Añade esto! (Carpeta de salida)
    "rootDir": "./src",               // ⚠️¡Añade esto! (Carpeta de entrada)
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "strict": true,
    "skipLibCheck": true,
    "sourceMap": true                 // Opcional: para debugging
  },
  "include": ["src/**/*.ts"],         // ⚠️¡Añade esto! (Qué archivos compilar)
  "exclude": ["node_modules"]         // Opcional: excluye dependencias
}